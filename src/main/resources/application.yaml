server:
  port: 8087
  servlet:
    context-path: /medconnect


spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/med_connect
    username: postgres
    password: postgres
    hikari:
      idle-timeout: 10000
      connectionTimeout: 20000
      minimumIdle: 5
      maximumPoolSize: 25
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    hiddenmethod:
      filter:
        enabled: true


api:
  imgur:
    url: https://api.imgur.com/
    albumHash: "k04aa3S"
    clientID: 8ce1a67ee460abc
    clientSecret: 02ec0abcae41871653935fe303043d743085b33d
    #accessToken expires 05.10.2023
    accessToken: aff9327d476e94cdc0f4f5432bd4956926422fd4







#logging:
  #level:
   # org:
     # springframework:
      #  security: DEBUG

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE